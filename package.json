{
  "name": "georgedallaswebsite",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "lint": "node scripts/lint.mjs",
    "typecheck": "node scripts/typecheck.mjs && pnpm --filter @georgedallas/cms typecheck && pnpm --filter @georgedallas/site typecheck",
    "test": "node --test \"packages/**/*.test.mjs\" \"apps/**/*.test.mjs\"",
    "build": "node scripts/build.mjs && pnpm --filter @georgedallas/site build && pnpm --filter @georgedallas/cms build && pnpm --filter @georgedallas/infra synth",
    "format": "node scripts/format-check.mjs",
    "local:up": "docker compose up -d postgres minio",
    "local:down": "docker compose down",
    "local:reset": "docker compose down -v && docker compose up -d postgres minio",
    "seed": "node scripts/seed-local.mjs"
  },
  "pnpm": {
    "overrides": {
      "esbuild@<0.28.1": "^0.28.1",
      "dompurify@<3.4.0": "^3.4.0",
      "postcss@<8.5.10": "^8.5.10",
      "yaml@>=2.0.0 <2.8.3": "^2.8.3"
    }
  }
}
