# Scripts

This directory contains project maintenance scripts, local seed helpers, and future import tooling.

Current commands are intentionally dependency-light so the foundation tickets can run from a clean checkout before Astro, Payload, and infrastructure packages are introduced.
